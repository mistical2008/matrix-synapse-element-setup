# Matrix server + element messenger setup script

## Requirements
- OS:
    - `Archlinux`
- Tools:
    - `wget`
    - `docker`, `docker-compose`
    - `Caddy`
    - `jq`

## Usage
- just run `./install.sh` and follow instructions
- after installing is finished, follow the setup instructions in the output

